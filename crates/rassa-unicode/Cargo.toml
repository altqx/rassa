[package]
name = "rassa-unicode"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[build-dependencies]
pkg-config = "0.3"

[dependencies]
rassa-core = { path = "../rassa-core" }
rassa-unibreak = { path = "../rassa-unibreak" }

[lib]
path = "src/lib.rs"
